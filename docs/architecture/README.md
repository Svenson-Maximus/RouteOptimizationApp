# Architecture Overview

Entry point for architecture documents.

## Document Map
- `context.md`: C4 system context (purpose only for now)
- `containers.md`: C4 container view (purpose only for now)
- `components.md`: C4 component view (purpose only for now)
- `deployment.md`: deployment view (purpose only for now)
- `database-structure.md`: relational DB structure and SQL model
- `../adr/`: architecture decision records
